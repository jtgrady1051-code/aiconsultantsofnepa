<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>FirstCall AI — AI Consultants of NEPA</title>
  <meta name="description" content="FirstCall AI is your 24/7 AI intake specialist. Never miss a contractor lead again. Built for trades, ready in days."/>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:ital,wght@0,300;0,400;0,600;1,300&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --black: #080808;
      --white: #f5f2eb;
      --acid: #FF6B2B;
      --mid: #1a1a1a;
      --dim: #2e2e2e;
      --muted: #888;
    }
    html { scroll-behavior: smooth; }
    body {
      background: var(--black);
      color: var(--white);
      font-family: 'DM Sans', sans-serif;
      font-weight: 300;
      overflow-x: hidden;
    }

    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 1.25rem 3rem;
      background: rgba(8,8,8,0.9);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255,107,43,0.12);
    }
    .nav-logo { font-family: 'Bebas Neue', sans-serif; font-size: 1.5rem; letter-spacing: 0.08em; color: var(--acid); text-decoration: none; }
    .nav-right { display: flex; gap: 1rem; align-items: center; }
    .nav-link { font-size: 0.75rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--muted); text-decoration: none; transition: color 0.2s; }
    .nav-link:hover { color: var(--white); }
    .nav-cta { background: var(--acid); color: var(--black); font-family: 'DM Sans', sans-serif; font-weight: 600; font-size: 0.8rem; letter-spacing: 0.1em; text-transform: uppercase; border: none; padding: 0.6rem 1.4rem; cursor: pointer; text-decoration: none; transition: transform 0.15s, box-shadow 0.15s; }
    .nav-cta:hover { transform: translateY(-2px); box-shadow: 0 6px 24px rgba(255,107,43,0.4); }

    .hero { min-height: 100vh; display: flex; flex-direction: column; justify-content: center; padding: 10rem 3rem 5rem; max-width: 1400px; margin: 0 auto; position: relative; overflow: hidden; }
    .hero-bg { position: absolute; inset: 0; z-index: 0; background: radial-gradient(ellipse 60% 55% at 85% 35%, rgba(255,107,43,0.08) 0%, transparent 70%), radial-gradient(ellipse 40% 40% at 5% 85%, rgba(255,107,43,0.04) 0%, transparent 60%); }
    .hero-grid { position: absolute; inset: 0; z-index: 0; opacity: 0.03; background-image: linear-gradient(var(--white) 1px, transparent 1px), linear-gradient(90deg, var(--white) 1px, transparent 1px); background-size: 60px 60px; }
    .hero-content { position: relative; z-index: 1; max-width: 1100px; }

    .eyebrow { font-size: 0.75rem; letter-spacing: 0.25em; text-transform: uppercase; color: var(--acid); margin-bottom: 1.5rem; display: flex; align-items: center; gap: 0.75rem; opacity: 0; animation: fadeUp 0.6s 0.2s forwards; }
    .eyebrow::before { content: ''; display: block; width: 30px; height: 1px; background: var(--acid); }

    h1 { font-family: 'Bebas Neue', sans-serif; font-size: clamp(4rem, 10vw, 9rem); line-height: 0.92; letter-spacing: 0.02em; margin-bottom: 2rem; opacity: 0; animation: fadeUp 0.7s 0.35s forwards; }
    h1 span { color: var(--acid); }

    .hero-sub { font-size: clamp(1rem, 1.8vw, 1.25rem); line-height: 1.65; color: rgba(245,242,235,0.65); max-width: 560px; margin-bottom: 2.5rem; opacity: 0; animation: fadeUp 0.7s 0.5s forwards; }
    .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; opacity: 0; animation: fadeUp 0.7s 0.65s forwards; }

    .btn-primary { background: var(--acid); color: var(--black); font-weight: 600; font-size: 0.85rem; letter-spacing: 0.12em; text-transform: uppercase; border: none; padding: 1rem 2.4rem; cursor: pointer; text-decoration: none; display: inline-block; transition: transform 0.15s, box-shadow 0.15s; }
    .btn-primary:hover { transform: translateY(-3px); box-shadow: 0 10px 32px rgba(255,107,43,0.45); }
    .btn-ghost { background: transparent; color: var(--white); font-weight: 400; font-size: 0.85rem; letter-spacing: 0.1em; text-transform: uppercase; border: 1px solid rgba(245,242,235,0.25); padding: 1rem 2.4rem; cursor: pointer; text-decoration: none; display: inline-block; transition: border-color 0.2s, color 0.2s; }
    .btn-ghost:hover { border-color: var(--acid); color: var(--acid); }

    .stats-bar { display: grid; grid-template-columns: repeat(5, 1fr); gap: 1px; background: var(--dim); border-top: 1px solid var(--dim); width: 100%; }
    .stat-item { background: var(--black); padding: 2rem; opacity: 0; animation: fadeUp 0.6s forwards; text-align: center; }
    .stat-item:nth-child(1) { animation-delay: 0.8s; }
    .stat-item:nth-child(2) { animation-delay: 0.9s; }
    .stat-item:nth-child(3) { animation-delay: 1.0s; }
    .stat-item:nth-child(4) { animation-delay: 1.1s; }
    .stat-item:nth-child(5) { animation-delay: 1.2s; }
    .stat-number { font-family: 'Bebas Neue', sans-serif; font-size: 2rem; color: var(--acid); line-height: 1; margin-bottom: 0.4rem; }
    .stat-label { font-size: 0.7rem; color: var(--muted); letter-spacing: 0.05em; line-height: 1.4; }

    section { padding: 6rem 3rem; max-width: 1400px; margin: 0 auto; width: 100%; }
    .section-label { font-size: 0.7rem; letter-spacing: 0.3em; text-transform: uppercase; color: var(--acid); margin-bottom: 1rem; }
    .section-title { font-family: 'Bebas Neue', sans-serif; font-size: clamp(2.5rem, 5vw, 4.5rem); line-height: 1; margin-bottom: 1.5rem; }
    .section-body { color: rgba(245,242,235,0.6); font-size: 1rem; line-height: 1.7; max-width: 480px; }

    .how { background: var(--mid); width: 100%; }
    .how-inner { max-width: 1100px; margin: 0 auto; padding: 6rem 3rem; }
    .how-header { margin-bottom: 4rem; }
    .steps { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 2px; background: var(--dim); }
    .step { background: var(--mid); padding: 2.5rem 2rem; transition: background 0.2s; }
    .step:hover { background: #222; }
    .step-num { font-family: 'Bebas Neue', sans-serif; font-size: 4.5rem; color: rgba(255,107,43,0.12); line-height: 1; margin-bottom: 1rem; }
    .step-icon { font-size: 1.5rem; margin-bottom: 0.75rem; }
    .step-title { font-weight: 600; font-size: 0.95rem; margin-bottom: 0.5rem; }
    .step-desc { font-size: 0.85rem; color: var(--muted); line-height: 1.6; }
    .step-badge { display: inline-block; margin-top: 0.75rem; font-size: 0.6rem; letter-spacing: 0.2em; text-transform: uppercase; color: var(--acid); border: 1px solid rgba(255,107,43,0.3); padding: 0.25rem 0.6rem; }

    .routing-section { background: var(--black); width: 100%; }
    .routing-inner { max-width: 1100px; margin: 0 auto; padding: 6rem 3rem; }
    .routing-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; background: var(--dim); margin-top: 3rem; }
    .route-card { background: var(--black); padding: 2.5rem 2rem; border-top: 3px solid transparent; transition: background 0.2s; }
    .route-card.hot { border-color: var(--acid); }
    .route-card.review { border-color: rgba(255,107,43,0.4); }
    .route-card.cold { border-color: var(--dim); }
    .route-dot { width: 10px; height: 10px; border-radius: 50%; margin-bottom: 1.25rem; }
    .route-dot.hot { background: var(--acid); }
    .route-dot.review { background: rgba(255,107,43,0.5); }
    .route-dot.cold { background: var(--muted); }
    .route-label { font-weight: 600; font-size: 1rem; margin-bottom: 0.5rem; }
    .route-desc { font-size: 0.85rem; color: var(--muted); line-height: 1.6; }

    .channels-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; background: var(--dim); margin-top: 3rem; }
    .channel-card { background: var(--mid); padding: 2.5rem 2rem; transition: background 0.2s; }
    .channel-card:hover { background: #222; }
    .channel-icon { font-size: 2rem; margin-bottom: 1.25rem; }
    .channel-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.8rem; letter-spacing: 0.04em; margin-bottom: 0.75rem; }
    .channel-desc { font-size: 0.875rem; color: var(--muted); line-height: 1.65; }

    /* PRICING */
    .pricing-section { background: var(--mid); width: 100%; }
    .pricing-inner { max-width: 1100px; margin: 0 auto; padding: 6rem 3rem; }
    .pricing-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; background: var(--dim); margin-top: 3rem; }

    .price-card { background: var(--black); padding: 2.5rem 2rem; position: relative; transition: background 0.2s; border-top: 2px solid transparent; display: flex; flex-direction: column; }
    .price-card:hover { background: #0d0d0d; }
    .price-card.featured { border-color: var(--acid); background: #0d0d0d; }
    .featured-badge { position: absolute; top: -1px; left: 50%; transform: translateX(-50%); background: var(--acid); color: var(--black); font-size: 0.6rem; font-weight: 600; letter-spacing: 0.15em; text-transform: uppercase; padding: 0.3rem 1rem; white-space: nowrap; }

    .price-tier { font-family: 'Bebas Neue', sans-serif; font-size: 2rem; letter-spacing: 0.04em; margin-bottom: 0.25rem; }
    .price-tag { font-size: 0.65rem; letter-spacing: 0.2em; text-transform: uppercase; color: var(--muted); margin-bottom: 1.25rem; }

    .price-monthly-row { display: flex; align-items: baseline; gap: 0.5rem; margin-bottom: 0.75rem; }
    .price-amount { font-family: 'Bebas Neue', sans-serif; font-size: 3.5rem; color: var(--acid); line-height: 1; }
    .price-per { font-size: 0.8rem; color: var(--muted); }

    .price-setup-row { display: flex; align-items: center; gap: 0.75rem; background: rgba(255,107,43,0.06); border: 1px solid rgba(255,107,43,0.18); padding: 0.65rem 1rem; margin-bottom: 1.5rem; }
    .price-setup-amount { font-family: 'Bebas Neue', sans-serif; font-size: 1.3rem; color: var(--acid); flex-shrink: 0; }
    .price-setup-label { font-size: 0.7rem; color: rgba(245,242,235,0.45); line-height: 1.4; }

    .price-divider { height: 1px; background: var(--dim); margin-bottom: 1.5rem; }

    .price-features { list-style: none; margin-bottom: 1.25rem; flex: 1; }
    .price-features li { font-size: 0.85rem; color: rgba(245,242,235,0.7); padding: 0.5rem 0; border-bottom: 1px solid rgba(255,255,255,0.04); display: flex; align-items: flex-start; gap: 0.6rem; line-height: 1.4; }
    .price-features li::before { content: '✓'; color: var(--acid); font-weight: 600; flex-shrink: 0; }

    .price-setup-covers { font-size: 0.71rem; color: var(--muted); line-height: 1.6; border-left: 2px solid rgba(255,107,43,0.2); padding-left: 0.75rem; margin-bottom: 1.75rem; }

    .btn-tier { display: block; width: 100%; text-align: center; background: var(--acid); color: var(--black); font-family: 'DM Sans', sans-serif; font-weight: 600; font-size: 0.8rem; letter-spacing: 0.12em; text-transform: uppercase; border: none; padding: 1rem; cursor: pointer; text-decoration: none; transition: transform 0.15s, box-shadow 0.15s; }
    .btn-tier:hover { transform: translateY(-2px); box-shadow: 0 8px 28px rgba(255,107,43,0.4); }
    .btn-tier-ghost { display: block; width: 100%; text-align: center; background: transparent; color: var(--white); font-family: 'DM Sans', sans-serif; font-weight: 400; font-size: 0.8rem; letter-spacing: 0.1em; text-transform: uppercase; border: 1px solid rgba(245,242,235,0.2); padding: 1rem; cursor: pointer; text-decoration: none; transition: border-color 0.2s, color 0.2s; }
    .btn-tier-ghost:hover { border-color: var(--acid); color: var(--acid); }
    .price-note { font-size: 0.7rem; color: var(--muted); text-align: center; margin-top: 0.75rem; }

    .integrations-bar { background: var(--black); border-top: 1px solid var(--dim); border-bottom: 1px solid var(--dim); padding: 2.5rem 3rem; }
    .integrations-inner { max-width: 1100px; margin: 0 auto; }
    .int-label { font-size: 0.65rem; letter-spacing: 0.3em; text-transform: uppercase; color: var(--muted); margin-bottom: 1.5rem; }
    .int-logos { display: flex; flex-wrap: wrap; gap: 1rem; align-items: center; }
    .int-pill { background: var(--mid); border: 1px solid var(--dim); font-size: 0.75rem; letter-spacing: 0.08em; color: rgba(245,242,235,0.6); padding: 0.5rem 1.25rem; }

    .wyg-section { background: var(--mid); width: 100%; }
    .wyg-inner { max-width: 1100px; margin: 0 auto; padding: 5rem 3rem; display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; align-items: center; }
    .wyg-title { font-family: 'Bebas Neue', sans-serif; font-size: clamp(2.5rem, 4vw, 3.8rem); line-height: 1; margin-bottom: 1.5rem; }
    .wyg-title span { color: var(--acid); }
    .wyg-body { font-size: 0.95rem; color: rgba(245,242,235,0.6); line-height: 1.8; margin-bottom: 1.5rem; }
    .wyg-list { list-style: none; display: flex; flex-direction: column; gap: 0.85rem; }
    .wyg-list li { font-size: 0.9rem; color: rgba(245,242,235,0.7); display: flex; align-items: flex-start; gap: 0.75rem; line-height: 1.5; }
    .wyg-list li::before { content: '→'; color: var(--acid); font-weight: 600; flex-shrink: 0; }

    .cta-section { width: 100%; background: var(--black); position: relative; overflow: hidden; }
    .cta-section::before { content: 'FIRSTCALL'; position: absolute; right: -4rem; top: 50%; transform: translateY(-50%); font-family: 'Bebas Neue', sans-serif; font-size: 18vw; color: rgba(255,107,43,0.03); pointer-events: none; white-space: nowrap; }
    .cta-center { max-width: 700px; margin: 0 auto; text-align: center; position: relative; z-index: 1; padding: 6rem 3rem; }
    .cta-center .section-title { font-size: clamp(2.5rem, 5vw, 4.5rem); }
    .cta-sub { font-size: 1rem; color: rgba(245,242,235,0.55); line-height: 1.7; margin: 1.25rem auto 2.5rem; max-width: 480px; }
    .cta-note { font-size: 0.75rem; color: var(--muted); margin-top: 1.25rem; }

    footer { border-top: 1px solid var(--dim); padding: 2rem 3rem; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; }
    .footer-logo { font-family: 'Bebas Neue', sans-serif; font-size: 1.2rem; color: var(--acid); letter-spacing: 0.08em; }
    .footer-text { font-size: 0.75rem; color: var(--muted); }
    .footer-text a { color: var(--muted); text-decoration: none; }
    .footer-text a:hover { color: var(--acid); }

    @keyframes fadeUp { from { opacity: 0; transform: translateY(22px); } to { opacity: 1; transform: translateY(0); } }

    @media (max-width: 900px) {
      .stats-bar { grid-template-columns: repeat(3, 1fr); }
      .pricing-grid { grid-template-columns: 1fr; }
      .routing-grid { grid-template-columns: 1fr; }
      .channels-grid { grid-template-columns: 1fr; }
      .wyg-inner { grid-template-columns: 1fr; gap: 2.5rem; padding: 4rem 1.5rem; }
    }
    @media (max-width: 768px) {
      nav { padding: 1rem 1.5rem; }
      .hero { padding: 8rem 1.5rem 3rem; }
      section { padding: 4rem 1.5rem; }
      .stats-bar { grid-template-columns: 1fr 1fr; }
      .how-inner, .routing-inner, .pricing-inner { padding: 4rem 1.5rem; }
      footer { flex-direction: column; text-align: center; }
      .nav-right .nav-link { display: none; }
    }
  </style>
</head>
<body>

  <nav>
    <a href="/" class="nav-logo">AI Consultants of NEPA</a>
    <div class="nav-right">
      <a href="/" class="nav-link">← Back to Site</a>
      <a href="/intake" class="nav-cta">Free Evaluation</a>
    </div>
  </nav>

  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-grid"></div>
    <div class="hero-content">
      <div class="eyebrow">AI Intake Specialist — Built for Contractors</div>
      <h1>Never Miss<br/>A Lead<br/><span>Again.</span></h1>
      <p class="hero-sub">FirstCall AI answers every call, web inquiry, and text — 24/7, no hold times, no voicemail. Leads scored, routed, and entered into your CRM automatically.</p>
      <div class="hero-actions">
        <a href="#pricing" class="btn-primary">See Pricing & Get Started</a>
        <a href="#how-it-works" class="btn-ghost">How It Works</a>
      </div>
    </div>
  </section>

  <div class="stats-bar">
    <div class="stat-item"><div class="stat-number">24/7</div><div class="stat-label">Lead Coverage</div></div>
    <div class="stat-item"><div class="stat-number">3 Min</div><div class="stat-label">Avg. Intake Time</div></div>
    <div class="stat-item"><div class="stat-number">10+</div><div class="stat-label">Trade Categories</div></div>
    <div class="stat-item"><div class="stat-number">Zero</div><div class="stat-label">Training Required</div></div>
    <div class="stat-item"><div class="stat-number">Auto</div><div class="stat-label">CRM Entry</div></div>
  </div>

  <div class="how" id="how-it-works">
    <div class="how-inner">
      <div class="how-header">
        <div class="section-label">Process</div>
        <div class="section-title">How It Works</div>
        <p class="section-body">Five steps from first contact to qualified lead in your CRM — fully automated, no human intervention required.</p>
      </div>
      <div class="steps">
        <div class="step"><div class="step-num">01</div><div class="step-icon">📞</div><div class="step-title">Lead Contacts You</div><div class="step-desc">Jamie answers every call, web inquiry, and text — 24/7, no hold times, no voicemail.</div><div class="step-badge">Always On</div></div>
        <div class="step"><div class="step-num">02</div><div class="step-icon">🏗️</div><div class="step-title">Trade-Aware Qualification</div><div class="step-desc">Right questions for each trade — roofing, kitchens, additions, new builds, storm damage, and more.</div><div class="step-badge">10 Trade Categories</div></div>
        <div class="step"><div class="step-num">03</div><div class="step-icon">⚡</div><div class="step-title">Lead Scored & Routed</div><div class="step-desc">Scored on project type, location, budget, timeline, and ownership. Routed in real time.</div><div class="step-badge">Smart Scoring</div></div>
        <div class="step"><div class="step-num">04</div><div class="step-icon">📋</div><div class="step-title">CRM Entry — Automatic</div><div class="step-desc">Leads flow directly into your CRM as new opportunities. Zero manual entry.</div><div class="step-badge">Zero Data Entry</div></div>
        <div class="step"><div class="step-num">05</div><div class="step-icon">🔔</div><div class="step-title">Instant Alert</div><div class="step-desc">Your team gets an alert the moment a hot lead lands — with all intake details ready.</div><div class="step-badge">Instant Alert</div></div>
      </div>
    </div>
  </div>

  <div class="routing-section">
    <div class="routing-inner">
      <div class="section-label">Lead Routing Logic</div>
      <div class="section-title">Every Lead Gets Handled</div>
      <p class="section-body">No lead falls through the cracks — not even the ones that aren't a fit.</p>
      <div class="routing-grid">
        <div class="route-card hot"><div class="route-dot hot"></div><div class="route-label">Hot Lead</div><div class="route-desc">Pushed to CRM + consultant alerted immediately. Full intake notes attached and ready for your team.</div></div>
        <div class="route-card review"><div class="route-dot review"></div><div class="route-label">Needs Review</div><div class="route-desc">Flagged for office review with full intake notes attached. Nothing gets lost in the shuffle.</div></div>
        <div class="route-card cold"><div class="route-dot cold"></div><div class="route-label">Not Qualified</div><div class="route-desc">Caller referred to a partner if applicable — logged and tracked. Never lost, never ignored.</div></div>
      </div>
    </div>
  </div>

  <section>
    <div class="section-label">Intake Channels</div>
    <div class="section-title">Every Way a Lead Can Reach You</div>
    <div class="channels-grid">
      <div class="channel-card"><div class="channel-icon">📞</div><div class="channel-name">Phone</div><div class="channel-desc">AI voice agent answers every call. Natural voice — not robotic. After-hours coverage with no voicemail dead ends. Call log and transcript on every lead.</div></div>
      <div class="channel-card"><div class="channel-icon">💬</div><div class="channel-name">Web Form</div><div class="channel-desc">Embedded chat widget on your site. Trade-specific qualification questions. Works 24/7 without anyone on your team touching it.</div></div>
      <div class="channel-card"><div class="channel-icon">📱</div><div class="channel-name">SMS / Text</div><div class="channel-desc">Two-way text intake for leads who prefer texting. Full qualification flow over SMS. Available on the Growth plan.</div></div>
    </div>
  </section>

  <div class="integrations-bar">
    <div class="integrations-inner">
      <div class="int-label">Integrates With</div>
      <div class="int-logos">
        <div class="int-pill">HubSpot CRM</div>
        <div class="int-pill">Vapi.ai</div>
        <div class="int-pill">Twilio SMS</div>
        <div class="int-pill">ElevenLabs Voice</div>
        <div class="int-pill">Your Existing CRM</div>
      </div>
    </div>
  </div>

  <!-- PRICING -->
  <div class="pricing-section" id="pricing">
    <div class="pricing-inner">
      <div class="section-label">Service Tiers</div>
      <div class="section-title">Simple, Honest Pricing</div>
      <p class="section-body">Month-to-month. No long-term contracts. Cancel any time. The one-time activation fee covers your entire tech stack — we purchase and configure everything on your behalf.</p>
      <div class="pricing-grid">

        <!-- STARTER -->
        <div class="price-card">
          <div class="price-tier">Starter</div>
          <div class="price-tag">Web Only</div>
          <div class="price-monthly-row">
            <div class="price-amount">$297</div>
            <div class="price-per">/ month</div>
          </div>
          <div class="price-setup-row">
            <div class="price-setup-amount">+ $497</div>
            <div class="price-setup-label">one-time activation fee<br/>covers full tech stack setup</div>
          </div>
          <div class="price-divider"></div>
          <ul class="price-features">
            <li>AI web intake form on your site</li>
            <li>Trade-specific qualification engine</li>
            <li>HubSpot CRM setup &amp; sync</li>
            <li>CRM lead auto-entry</li>
            <li>Email notification on new leads</li>
          </ul>
          <div class="price-setup-covers">Activation covers: HubSpot configuration, web form build &amp; embed, CRM sync setup, and go-live testing.</div>
          <a href="https://buy.stripe.com/cNidR3f3v3e6eXd0aWgEg00" class="btn-tier-ghost">Get Started →</a>
          <div class="price-note">$794 due today · $297/mo starting month 2</div>
        </div>

        <!-- PRO -->
        <div class="price-card featured">
          <div class="featured-badge">★ Most Popular</div>
          <div class="price-tier">Pro</div>
          <div class="price-tag">Web + Phone</div>
          <div class="price-monthly-row">
            <div class="price-amount">$497</div>
            <div class="price-per">/ month</div>
          </div>
          <div class="price-setup-row">
            <div class="price-setup-amount">+ $597</div>
            <div class="price-setup-label">one-time activation fee<br/>covers full tech stack setup</div>
          </div>
          <div class="price-divider"></div>
          <ul class="price-features">
            <li>Everything in Starter</li>
            <li>AI phone agent (Jamie) — live 24/7</li>
            <li>Natural voice — not robotic</li>
            <li>After-hours coverage, no voicemail</li>
            <li>Call log &amp; transcript on every lead</li>
          </ul>
          <div class="price-setup-covers">Activation covers: Everything in Starter + Vapi.ai voice agent build, ElevenLabs voice configuration, call flow testing, and live QA.</div>
          <a href="https://buy.stripe.com/7sY00d8F78yq3ev3n8gEg01" class="btn-tier">Get Started →</a>
          <div class="price-note">$1,094 due today · $497/mo starting month 2</div>
        </div>

        <!-- GROWTH -->
        <div class="price-card">
          <div class="price-tier">Growth</div>
          <div class="price-tag">Full Stack</div>
          <div class="price-monthly-row">
            <div class="price-amount">$797</div>
            <div class="price-per">/ month</div>
          </div>
          <div class="price-setup-row">
            <div class="price-setup-amount">+ $697</div>
            <div class="price-setup-label">one-time activation fee<br/>covers full tech stack setup</div>
          </div>
          <div class="price-divider"></div>
          <ul class="price-features">
            <li>Everything in Pro</li>
            <li>SMS / text intake channel</li>
            <li>Monthly lead performance report</li>
            <li>Quarterly tune-up &amp; prompt updates</li>
            <li>Priority support</li>
          </ul>
          <div class="price-setup-covers">Activation covers: Everything in Pro + Twilio SMS configuration, two-way text flow build, reporting dashboard setup, and full stack QA.</div>
          <a href="https://buy.stripe.com/bJe28l6wZaGy5mD5vggEg02" class="btn-tier-ghost">Get Started →</a>
          <div class="price-note">$1,494 due today · $797/mo starting month 2</div>
        </div>

      </div>
    </div>
  </div>

  <div class="wyg-section">
    <div class="wyg-inner">
      <div>
        <div class="section-label">Why FirstCall AI</div>
        <div class="wyg-title">Most Contractors Miss <span>20–30% of Leads.</span></div>
        <p class="wyg-body">Every missed call, unanswered web inquiry, or text that went to voicemail is a job you didn't get a chance to bid on.</p>
        <p class="wyg-body">FirstCall AI captures every one — most clients see ROI within the first 30 days.</p>
      </div>
      <ul class="wyg-list">
        <li>Setup in days, not months — fully live within one week</li>
        <li>No long-term contracts — month-to-month, cancel any time</li>
        <li>Fully managed — we tune, monitor, and update monthly</li>
        <li>Built for contractors — roofing, remodels, new builds, storm damage</li>
        <li>Zero training required — if you can answer a phone, you're ready</li>
      </ul>
    </div>
  </div>

  <section class="cta-section">
    <div class="cta-center">
      <div class="section-label">Get Started</div>
      <div class="section-title">Ready to Stop Missing Leads?</div>
      <p class="cta-sub">Setup takes less than a week. No technical knowledge required on your end. We handle everything — configuration, testing, CRM connection, and go-live.</p>
      <a href="/intake" class="btn-primary">Schedule Your Free Call →</a>
      <div class="cta-note">Free call · No commitment · Setup in under a week</div>
    </div>
  </section>

  <footer>
    <div class="footer-logo">AI Consultants of NEPA</div>
    <div class="footer-text">
      © 2025 AI Consultants of NEPA &nbsp;·&nbsp;
      <a href="tel:+15702185903">(570) 218-5903</a> &nbsp;·&nbsp;
      <a href="mailto:info@aiconsultantsofnepa.com">info@aiconsultantsofnepa.com</a> &nbsp;·&nbsp;
      <a href="/">aiconsultantsofnepa.com</a>
    </div>
  </footer>

  <script>
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.step, .route-card, .channel-card, .price-card, .wyg-list li').forEach(el => {
      el.style.opacity = '0';
      el.style.transform = 'translateY(20px)';
      el.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
      observer.observe(el);
    });
  </script>

</body>
</html>
